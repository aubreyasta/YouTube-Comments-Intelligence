google-api-python-client>=2.100
youtube-transcript-api>=0.6
langdetect>=1.0.9
pandas>=2.0
markdown>=3.5

# PDF Output. Alternatives tried in this order if playwright is absent:
# weasyprint>=68, then pdfkit>=1.0.0. See docs/setup.md#pdf-engine for why.
playwright>=1.49
#   playwright install chromium
