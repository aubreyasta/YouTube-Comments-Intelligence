google-api-python-client>=2.100
google-genai>=0.3
youtube-transcript-api>=0.6
langdetect>=1.0.9
pandas>=2.0
markdown>=3.5

# Emotion stage only. Drop both if EMOTION_MODE is None in config.py.
transformers>=4.40
torch>=2.0

# PDF output
playwright>=1.40   # then: playwright install chromium